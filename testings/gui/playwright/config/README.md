# Playwright Config

Scoped to Playwright run config only (browser projects, timeouts, retries).
Reads from platform/configuration and platform/environments — not a
parallel source of truth for env/config values.
