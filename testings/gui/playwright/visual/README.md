# Visual Regression

Baseline screenshots and visual comparison specs, using Playwright's
built-in screenshot comparison. Nested here rather than as a separate
top-level engine — revisit if visual testing outgrows this scope.
