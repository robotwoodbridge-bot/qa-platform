# API Clients (test setup)

Backend calls used to seed/verify state during UI tests (e.g. create test
accounts via API instead of driving the UI). Not the API testing engine —
see testing/api/ for that.
