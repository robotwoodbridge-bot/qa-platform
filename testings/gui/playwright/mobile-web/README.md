# Playwright Mobile Web

Playwright device-emulation projects/tests for responsive mobile web
(viewport, touch, user-agent emulation). This is browser-based emulation
only — NOT native iOS/Android app automation. See testing/gui/robot/mobile
for native app testing.
