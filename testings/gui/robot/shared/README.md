# Robot Shared

Common .resource files, variables, and library config reused across
robot/web, robot/mobile/ios, and robot/mobile/android. Keeps each
platform folder scoped to its own tests/pages only.
