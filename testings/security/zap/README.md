# OWASP ZAP

- scans/    — active/passive scan configs and policies
- tests/    — specs that trigger/orchestrate ZAP scans and assert on results
- reports/  — generated scan output (artifacts, not source-controlled long-term)
- rules/    — custom alert rules, false-positive suppressions
- shared/   — auth handling for authenticated scans, fixtures, utils
