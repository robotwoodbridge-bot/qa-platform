# Utilities

Scope: retry/wait helpers, common assertions, shared small helpers only.
Not a general dumping ground — anything with a clear domain (data, storage,
notifications, etc.) belongs in its own module.
